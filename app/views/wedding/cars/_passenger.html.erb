<%= content_tag(:li, id: "car-#{car.id}-passenger-#{passenger.id}") do %>
  <%= "#{passenger.first_name} #{passenger.family_name}" %>
  (<%= phone_to(passenger.phone) + mail_to_or_default(passenger.email, default: '', prefix: ' / ') %>)
<% end %>