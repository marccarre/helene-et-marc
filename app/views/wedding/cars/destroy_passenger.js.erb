$('#notice')
  .empty()
  .removeClass('hidden')
  .addClass('visible')
  .text("<%= t('carpooling.car_successfully_deleted') %>");

$('#<%= "car-#{passenger.car.id}-passenger-#{passenger.id}" %>').fadeOut();
